<div class="w3-container w3-center w3-margin">
    <h3>Puntaje actual: {{puntaje}}</h3>

    <p>Temporizador: <span id="tiempo">10</span></p>

    {{#partida}}
        <p>Categoria: {{pregunta.categoria}}</p>
        <h2>Pregunta: {{pregunta.pregunta}}</h2>

        <form method="POST" action="/game/jugarPartida?action=procesar">
            <div class="w3-padding">

                {{#respuestas}}
                    <button class="w3-button w3-blue w3-margin-bottom" type="submit" name="idRespuesta" value="{{id}}">
                        {{texto}}
                    </button>
                    <br>
                {{/respuestas}}

            </div>
        </form>
    {{/partida}}

    <script>
        let tiempo = 10;
        const temporizador = setInterval(() => {
            tiempo--;
            document.querySelector('#tiempo').textContent = tiempo;

            if (tiempo <= 0) {
                clearInterval(temporizador);
                document.querySelector('form').submit();
            }
        }, 1000);
    </script>
</div>